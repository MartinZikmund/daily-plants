<UserControl x:Class="DailyPlants.Controls.AchievementNotification"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="using:DailyPlants.Controls"
             xmlns:helpers="using:DailyPlants.Helpers">

    <UserControl.Resources>
        <local:HexToColorConverter x:Key="HexToColorConverter" />
    </UserControl.Resources>

    <Grid x:Name="RootGrid"
          Visibility="Collapsed"
          HorizontalAlignment="Center"
          VerticalAlignment="Top"
          Margin="0,16,0,0">

        <Grid.RenderTransform>
            <TranslateTransform x:Name="SlideTransform" Y="-100" />
        </Grid.RenderTransform>

        <Border Background="{ThemeResource AcrylicBackgroundFillColorDefaultBrush}"
                CornerRadius="12"
                Padding="16"
                MinWidth="300"
                MaxWidth="400">
            <Border.Shadow>
                <ThemeShadow />
            </Border.Shadow>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <!-- Achievement Icon -->
                <Border x:Name="IconBorder"
                        Grid.Column="0"
                        Width="56" Height="56"
                        CornerRadius="28"
                        Margin="0,0,16,0">
                    <FontIcon x:Name="AchievementIcon"
                              FontSize="28"
                              Foreground="White"
                              HorizontalAlignment="Center"
                              VerticalAlignment="Center" />
                </Border>

                <!-- Achievement Info -->
                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                    <TextBlock Text="{helpers:Localize Key=Achievement_Unlocked}"
                               Style="{StaticResource CaptionTextBlockStyle}"
                               Foreground="{ThemeResource AccentFillColorDefaultBrush}"
                               FontWeight="SemiBold" />
                    <TextBlock x:Name="AchievementName"
                               Style="{StaticResource BodyStrongTextBlockStyle}"
                               TextWrapping="Wrap"
                               Margin="0,2,0,0" />
                    <TextBlock x:Name="AchievementDescription"
                               Style="{StaticResource CaptionTextBlockStyle}"
                               Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                               TextWrapping="Wrap"
                               MaxLines="2"
                               Margin="0,2,0,0" />
                </StackPanel>

                <!-- Close Button -->
                <Button Grid.Column="2"
                        Click="CloseButton_Click"
                        Width="32" Height="32"
                        Padding="0"
                        Background="Transparent"
                        VerticalAlignment="Top">
                    <FontIcon Glyph="&#xE711;" FontSize="12" />
                </Button>
            </Grid>
        </Border>
    </Grid>

</UserControl>
